distro() {
  if [ -f /etc/apk/repositories ]; then
    echo "alpine"
  elif command -v brew &>/dev/null; then
    echo "osx"
  elif [ -f /etc/portage/make.conf ]; then
    echo "gentoo"
  else
    echo "unknown"
  fi
}
